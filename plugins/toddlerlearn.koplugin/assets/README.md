# Assets

This folder is empty (just placeholder subfolders) — see
`CONTENT_GUIDE.md` in the project root for image specs and how to add
images here.

Subfolders expected by the default `content.lua`:

- `animals/` — cat.png, dog.png, cow.png, bird.png
- `fruit/` — apple.png, banana.png, grapes.png
- `colors/` — red.png, blue.png, green.png
- `numbers/` — 1.png, 2.png, 3.png
- `letters/` — a.png, b.png, c.png

For Phase 2 testing, any placeholder PNGs with these exact names will do
— they don't need to look right yet, just exist so the layout code has
something to load.
